---
description: Backend conventions — Express layered architecture, Zod, Prisma
globs: backend/**
alwaysApply: false
---

# Backend

```
routes → controllers → services → Prisma
```

- Zod on routes, `sendSuccess`/`sendNotFound` in controllers
- Prisma only in services; orders use `$transaction`
- `app.ts` exports app (no listen); `server.ts` local dev; `api/index.ts` Vercel
- `loadEnv.ts` first in `app.ts`; CORS from `ALLOWED_ORIGINS`

New API: service → controller → route → `app.ts` → `docs/API.md`
